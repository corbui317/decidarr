services:
  decidarr:
    build: .
    container_name: decidarr
    restart: unless-stopped
    ports:
      - "${PORT:-3100}:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongo:27017/decidarr
      - SECURE_COOKIES=true
      - DECIDARR_SECRET=${DECIDARR_SECRET}
      - DECIDARR_SETUP_SECRET=${DECIDARR_SETUP_SECRET}
      - DECIDARR_ALLOW_PRIVATE_URLS=${DECIDARR_ALLOW_PRIVATE_URLS:-true}
    depends_on:
      - mongo
    networks:
      - decidarr-network

  mongo:
    image: mongo:7
    container_name: decidarr-mongo
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    networks:
      - decidarr-network

volumes:
  mongo-data:

networks:
  decidarr-network:
    driver: bridge
