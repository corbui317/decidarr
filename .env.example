# Decidarr Configuration
# =====================
# Most settings are now configured via the web UI after first run.
# This file is only needed if you want to customize the port.

# Optional - Change the exposed port (default: 3100)
PORT=3100

# Note: Plex token, TMDB API key, and sync settings are all
# configured through the Settings UI in the application.
# Encryption keys are automatically generated on first run
# and securely stored in the database.
